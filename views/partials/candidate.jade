article.candidate
  h1 
    | {{candidate.full_name}} {{candidate.last_name}}
    //-img.img-cirlce.headshot.thumb.candidate-thumb(src="/userfiles/{{candidate.user_photo}}")
  .profile-qa(ng-repeat="question in candidate.questions", question="{{question.type}}" )
    | QUESTION BODY